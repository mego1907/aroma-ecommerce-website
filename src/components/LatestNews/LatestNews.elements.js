import styled from 'styled-components';
import {Container} from '../../GlobalStyle';

export const LNewsArticles = styled.section`
  margin: 2rem 0;
`;

export const LNewsContainer = styled(Container)``;

export const Articles = styled.div`
  display: flex;
  flex-wrap: wrap;
  justify-content: center;
`;